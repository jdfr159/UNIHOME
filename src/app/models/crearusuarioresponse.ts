export interface CrearUsuarioResponse {
    message: string;
    user: any;  // Puedes reemplazar `any` con la estructura específica de `user` si la conoces
    persona: any; // Igual para `persona`
  }